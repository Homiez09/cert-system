import { Kanit, Abel } from "next/font/google";

export const kanit = Kanit({
    weight: "400",
    subsets: ["latin", "thai"],
});

export const abel = Abel({
    weight: "400",
    subsets: ["latin"]
})